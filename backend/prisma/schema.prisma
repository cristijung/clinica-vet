datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Pet {
  id      Int    @id @default(autoincrement())
  nome    String
  especie String
  idade   Int
}